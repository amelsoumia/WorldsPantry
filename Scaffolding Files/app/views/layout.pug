//- Base structure for all pages 

doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    title World's Pantry
    link(rel="stylesheet", href="/style.css")

  body
    header
      nav
        .nav-left
          a(href="/")
            img(src="/images/logo.png", alt="Logo")
          a(href="/") Home
          a(href="/explore") Browse

        .nav-center
          form.search-form(action="/explore", method="GET")
            input.search-input(
              type="text"
              name="search"
              placeholder="Browse recipes"
              value=selectedSearch || ""
            )
            button(type="submit") Search

        .nav-right
          if user
            a(href="/profile")
              img(src="/images/default-user-icon.png", alt="Profile")
            a(href="/auth/signout") Log out
          else
            a(href="/login") Sign In
            a(href="/auth/signup") Sign Up

    main
      block content
